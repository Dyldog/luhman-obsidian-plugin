import { LanguageKeys, LanguageMapping } from "languageModel";

export const en_us: LanguageMapping = {
    [LanguageKeys.checkSettingsMessage]: "Try checking the settings if this seems wrong.",
    test: (data)=>``
}